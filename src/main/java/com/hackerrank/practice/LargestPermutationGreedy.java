package com.hackerrank.practice;

import com.sun.org.apache.xpath.internal.operations.Bool;

public class LargestPermutationGreedy {
    public static void main(String[] args) {
        LargestPermutationGreedy largestPermutationGreedy = new LargestPermutationGreedy();
//        largestPermutationGreedy.test(1, new int[]{1, 2, 3, 4});

        Boolean s = true;
        System.out.println("1".equals(null));
        System.out.println(Boolean.TRUE.equals(true));
        System.out.println(Boolean.TRUE.equals(s));
        System.out.println(Boolean.parseBoolean("1"));
    }



   /* private void test(int k, int[] arr) {
        int[] ints = Arrays.copyOf(arr, arr.length);
        Arrays.sort(ints);
        int lastIndexUsed = 0;
        while (k > 0) {
//            int ints[ ints.length - 1 - lastIndexUsed]
        }

    }*/
}
